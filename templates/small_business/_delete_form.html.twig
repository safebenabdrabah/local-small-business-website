<form method="post" action="{{ path('app_small_business_delete', {'id': small_business.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ small_business.id) }}">
    <button class="btn">Delete</button>
</form>
